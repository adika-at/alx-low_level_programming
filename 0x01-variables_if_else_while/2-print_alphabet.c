#include <stdio.h>

/**
 * main - Entry point
 * Description: 'Prints the alphabet in lowercase.'
 * Return: Always 0.
 */

int main(void)
{
	char alphabetic;

	for (alphabetic 'a'; alphabetic <= 'z'; alphabetic++)
		printf(alphabetic);
	printf("\n");
	return (0);
}
