#include <stdlib.h>
#include <time.h>
/* this is my first comment */

/* then this follows */
int main(void)
{
		int n;

			srand(time(0));
				n = rand() - RAND_MAX / 2;
					/*my code */
					return (0);
}
