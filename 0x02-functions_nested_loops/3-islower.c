#include "main.h"

/**
 * main - check the code.
 * c - this  is an integer representing the lowercase character
 * Return: Always 0.
 */

int main(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	return (0);
}
